# Product

## 用户目标

运营、数据生产、算法和交付人员需要快速判断一项能力是什么、能否复用、输入输出是什么，以及它在业务链路中的位置。

## 产品模型

- Handler / 算子：最小执行单元，完成一件具体事情。
- Flow：由多个 Handler 组成、可独立登记和复用的局部能力。
- Pipeline：编排后的完整业务链路，可包含 Flow、Handler 和内联步骤。
- 旧源码 `aigc.Flow`：新模型中的 Pipeline 候选。历史上拆开的多个旧 Flow 可能需要归并。

Handler、Flow 和 Pipeline 是三类对象，不共用 ID 命名空间，不把内联逻辑伪造成 Handler。

## 事实边界

- 源码扫描结果是自动来源，但自动识别不等于业务确认。
- 无法确认的关系标记为 `unknown` 或 `needs_confirmation`。
- 无真实统计的成本和资源只显示“待统计（占位数据）”。
- 前台不展示源码路径、提交哈希、凭证或 Wiki 原始双链。
- 目录写入和测试执行在后端 API 接入前保持禁用。

## 交互原则

- 延续三栏、数据密集型后台结构。
- Handler、Flow、Pipeline 使用图标、名称和颜色共同区分。
- 移动端在列表和详情间切换，不挤压双栏。
- 长 ID、路径和错误信息允许换行或横向滚动。
- 不依赖颜色单独表达状态，操作具备可见焦点和足够点击面积。
